# Historical stock-import notes

This folder contains source material from the 2026-08-29 stock import. It is retained only as provenance for the catalogue data and images.

The current application order flow is defined by the source files under `lib/` and `app/api/`. Econt shipment creation uses the production API and is controlled by the `ECONT_CREATE_ENABLED` environment variable.
